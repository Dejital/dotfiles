# Keep my username/password elsewhere
source ~/.Private/muttrc

source ~/.mutt/mailboxes
folder-hook gmail set from="sergey@sergeyk.com"
set mbox_type=Maildir
set folder=$HOME/Mail
set spoolfile=+gmail/INBOX

set certificate_file=~/.mutt/certificates

# Alias stuff
set alias_file= ~/.mutt/aliases
set sort_alias= alias
set reverse_alias=yes
source $alias_file

set move = no

# Keybindings from:
# http://crunchbanglinux.org/wiki/howto/howto_setup_mutt_with_gmail_imap
bind editor <space> noop
macro index gi "<change-folder>=gmail/INBOX<enter>" "Go to inbox"
macro index ga "<change-folder>=gmail/archive<enter>" "Go to all mail"
macro index gs "<change-folder>=gmail/sent<enter>" "Go to Sent Mail"
macro index gd "<change-folder>=gmail/drafts<enter>" "Go to drafts"
macro index gf "<change-folder>=gmail/flagged<enter>" "Go to starred"
macro index gr "<change-folder>=gmail/Rockhurst<enter>" "Go to Rockhurst filter"

# For better looks
set markers=no # don't put '+' at the beginning of wrapped lines
set pager_index_lines= 5 # how large is the index window?
set sort = 'threads'
set sort_aux = 'last-date-received'

# Color scheme
source ~/.mutt/themes/linux.muttrc

# Viewing URLs
macro index cb |urlview 'call urlview to extract URLs out of a message'
macro pager cb |urlview 'call urlview to extract URLs out of a message' 
#macro pager \cb <pipe-entry>"urlview"<enter> 'Follow links with urlview'

# My Editor
set editor='vim + -c "set textwidth=72" -c "set wrap" -c "set nocp" -c "set spell"'
